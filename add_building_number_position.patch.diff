diff --git a/address_parser/extractor.py b/address_parser/extractor.py
index 1111111..2222222 100644
--- a/address_parser/extractor.py
+++ b/address_parser/extractor.py
@@
 from dataclasses import dataclass, field
 from typing import Optional
@@
 @dataclass
 class ExtractionResult:
     raw_address:      str
     building_number:  Optional[str]
+    number_index:     Optional[int]  # 0-based index of 1st char of building_number in raw_address
     confidence:       float           # 0.0 – 1.0
     lang_code:        Optional[str]
     number_position:  str             # 'first' | 'last' | 'complex' | 'unknown'
     method:           str
     notes:            list = field(default_factory=list)
@@
 def _remove_blocked_patterns(text: str) -> str:
@@
     return re.sub(r"\s+", " ", text).strip()
+
+def _mask_blocked_patterns(text: str) -> str:
+    """Mask (do not remove) floor numbers / PO Box / ordinals with spaces.
+
+    Why masking?
+    - We want to keep character positions aligned with the original Omega
+      concatenation so we can output the start index of the extracted number.
+    """
+    from .patterns import RE_FLOOR, RE_POBOX, RE_ORDINAL
+
+    def _mask(regex, s):
+        return regex.sub(lambda m: " " * len(m.group(0)), s)
+
+    text = _mask(RE_FLOOR, text)
+    text = _mask(RE_POBOX, text)
+    text = _mask(RE_ORDINAL, text)
+    return text
@@
 class InternationalBuildingNumberExtractor:
@@
     def extract(self, addr_ln3: str, addr_ln4: str = "") -> ExtractionResult:
@@
-        parts = [_clean(str(addr_ln3 or "")), _clean(str(addr_ln4 or ""))]
+        parts = [_clean(str(addr_ln3 or "")), _clean(str(addr_ln4 or ""))]
         parts = [p for p in parts if p]
-        full_address = " ".join(parts)
-        # Remove floor numbers, PO boxes, postal codes, ordinals
-        full_address = _remove_blocked_patterns(full_address)
-        if not full_address:
+        raw_concat = " ".join(parts)  # audit trail = Omega LN3 + LN4
+
+        # Mask blocked patterns so extraction ignores them BUT indices remain
+        # aligned with the original concatenation.
+        work = _mask_blocked_patterns(raw_concat)
+
+        # If masking produced leading spaces (blocked pattern at start), keep
+        # a left-strip view for matching while preserving a positional offset.
+        work_stripped = work.lstrip()
+        lead_offset = len(work) - len(work_stripped)
+        work = work_stripped
+
+        if not work:
             return ExtractionResult(
-                raw_address="", building_number=None,
+                raw_address=raw_concat, building_number=None,
+                number_index=None,
                 confidence=0.0, lang_code=None,
                 number_position="unknown", method="empty input",
             )
@@
-        m = RE_INLINE_NO.search(full_address)
+        m = RE_INLINE_NO.search(work)
         if m:
             num = _normalize_number(m.group(1))
             return ExtractionResult(
-                raw_address=full_address, building_number=num,
+                raw_address=raw_concat, building_number=num,
+                number_index=lead_offset + m.start(1),
                 confidence=0.97, lang_code=None,
                 number_position="inline", method="inline No./nº/nr. pattern",
             )
@@
-        m = RE_AFTER_COMMA.search(full_address)
+        m = RE_AFTER_COMMA.search(work)
         if m:
             num = _normalize_number(m.group(1))
             return ExtractionResult(
-                raw_address=full_address, building_number=num,
+                raw_address=raw_concat, building_number=num,
+                number_index=lead_offset + m.start(1),
                 confidence=0.93, lang_code=None,
                 number_position="after_comma", method="after-comma number pattern",
             )
@@
-        m = RE_RU_DOM.search(full_address)
+        m = RE_RU_DOM.search(work)
         if m:
             num = _normalize_number(m.group(1))
             return ExtractionResult(
-                raw_address=full_address, building_number=num,
+                raw_address=raw_concat, building_number=num,
+                number_index=lead_offset + m.start(1),
                 confidence=0.96, lang_code="RU",
                 number_position="last", method="Russian dom (д./дом) indicator",
             )
@@
-        m = RE_JP_CHOME.search(full_address)
+        m = RE_JP_CHOME.search(work)
         if m:
             num = f"{m.group(1)}-{m.group(2)}-{m.group(3)}"
             return ExtractionResult(
-                raw_address=full_address, building_number=num,
+                raw_address=raw_concat, building_number=num,
+                number_index=lead_offset + m.start(1),
                 confidence=0.95, lang_code="JP",
                 number_position="complex", method="Japanese chome-ban-go",
             )
-        m = RE_JP_SIMPLE.search(full_address)
+        m = RE_JP_SIMPLE.search(work)
         if m:
             num = f"{m.group(1)}-{m.group(2)}"
             return ExtractionResult(
-                raw_address=full_address, building_number=num,
+                raw_address=raw_concat, building_number=num,
+                number_index=lead_offset + m.start(1),
                 confidence=0.93, lang_code="JP",
                 number_position="complex", method="Japanese ban-go",
             )
@@
-        m = RE_ZH_NUMBER.search(full_address)
+        m = RE_ZH_NUMBER.search(work)
         if m:
             return ExtractionResult(
-                raw_address=full_address, building_number=m.group(1),
+                raw_address=raw_concat, building_number=m.group(1),
+                number_index=lead_offset + m.start(1),
                 confidence=0.90, lang_code="ZH",
                 number_position="complex", method="Chinese 号 indicator",
             )
@@
-        m = RE_KO_NUMBER.search(full_address)
+        m = RE_KO_NUMBER.search(work)
         if m:
             return ExtractionResult(
-                raw_address=full_address, building_number=m.group(1),
+                raw_address=raw_concat, building_number=m.group(1),
+                number_index=lead_offset + m.start(1),
                 confidence=0.90, lang_code="KO",
                 number_position="complex", method="Korean address indicator",
             )
@@
-        detection = detect_number_position(full_address)
+        detection = detect_number_position(work)
         position  = detection["position"]
         lang_code = detection["lang_code"]
@@
-            m = RE_NUMBER_FIRST.match(full_address)
+            m = RE_NUMBER_FIRST.match(work)
             if m:
                 num = _normalize_number(m.group(1))
                 return ExtractionResult(
-                    raw_address=full_address, building_number=num,
+                    raw_address=raw_concat, building_number=num,
+                    number_index=lead_offset + m.start(1),
                     confidence=0.95, lang_code=lang_code,
                     number_position="first",
                     method=f"number-first ({detection['method']})",
                 )
@@
-            m = RE_NUMBER_LAST.search(full_address)
+            m = RE_NUMBER_LAST.search(work)
             if m:
                 num = _normalize_number(m.group(1))
                 conf = 0.90 if position == "last" else 0.72
                 return ExtractionResult(
-                    raw_address=full_address, building_number=num,
+                    raw_address=raw_concat, building_number=num,
+                    number_index=lead_offset + m.start(1),
                     confidence=conf, lang_code=lang_code,
                     number_position="last",
                     method=f"number-last ({detection['method']})",
                 )
@@
-        m = re.search(r"[\s,]+(\d+[a-zA-Z]?(?:/[a-zA-Z])?)[\s,]*$", full_address)
+        m = re.search(r"[\s,]+(\d+[a-zA-Z]?(?:/[a-zA-Z])?)[\s,]*$", work)
         if m:
             num = _normalize_number(m.group(1))
             return ExtractionResult(
-                raw_address=full_address, building_number=num,
+                raw_address=raw_concat, building_number=num,
+                number_index=lead_offset + m.start(1),
                 confidence=0.75, lang_code=lang_code,
                 number_position="last",
                 method="trailing number heuristic",
             )
@@
-        m = RE_NUMBER_FIRST.match(full_address)
+        m = RE_NUMBER_FIRST.match(work)
         if m:
             num = _normalize_number(m.group(1))
             return ExtractionResult(
-                raw_address=full_address, building_number=num,
+                raw_address=raw_concat, building_number=num,
+                number_index=lead_offset + m.start(1),
                 confidence=0.65, lang_code=lang_code,
                 number_position="first",
                 method="number-first fallback",
             )
@@
-        candidates = RE_FALLBACK.findall(full_address)
-        if candidates:
-            # Filter out obvious postal codes or year-like 4+ digit numbers if
-            # there are shorter alternatives
-            short = [c for c in candidates if len(re.sub(r"\D", "", c)) <= 5]
-            pick = short[0] if short else candidates[0]
+        iters = list(RE_FALLBACK.finditer(work))
+        if iters:
+            candidates = [(m.group(1), m.start(1)) for m in iters]
+            short = [(c, p) for (c, p) in candidates if len(re.sub(r"\D", "", c)) <= 5]
+            pick, pick_pos = short[0] if short else candidates[0]
             return ExtractionResult(
-                raw_address=full_address, building_number=pick,
+                raw_address=raw_concat, building_number=pick,
+                number_index=lead_offset + pick_pos,
                 confidence=0.45, lang_code=lang_code,
                 number_position="fallback",
                 method="last-resort isolated number",
                 notes=["Low confidence — multiple numbers found; first selected"]
-                      if len(candidates) > 1 else [],
+                      if len(iters) > 1 else [],
             )
@@
         return ExtractionResult(
-            raw_address=full_address, building_number=None,
+            raw_address=raw_concat, building_number=None,
+            number_index=None,
             confidence=0.0, lang_code=lang_code,
             number_position="unknown", method="no number found",
         )
\ No newline at end of file

diff --git a/excel_processor.py b/excel_processor.py
index 3333333..4444444 100644
--- a/excel_processor.py
+++ b/excel_processor.py
@@
 OUTPUT COLUMNS ADDED
 --------------------
   BUILDING_NUMBER   → extracted number (e.g. "12", "12A", "12bis", "10-12")
+  BUILDING_NUMBER_POS → position (1-based) of the first character of BUILDING_NUMBER in FULL_ADDRESS
   BN_CONFIDENCE     → float 0.0-1.0 indicating extraction reliability
   BN_LANG           → detected language/script code
   BN_METHOD         → human-readable description of the extraction method
   FULL_ADDRESS      → concatenated CLIADDLN3_LA + CLIADDLN4_LA (for audit)
@@
 def process_excel(
@@
     col4:        str  = "CLIADDLN4_LA",
     out_col:     str  = "BUILDING_NUMBER",
+    pos_col:     str  = "BUILDING_NUMBER_POS",
+    add_position: bool = True,
     add_confidence: bool = True,
     add_lang:       bool = True,
     add_method:     bool = True,
     add_full_addr:  bool = True,
 ) -> pd.DataFrame:
@@
-    df[out_col] = [r.building_number if r.building_number else "" for r in results]
+    df[out_col] = [r.building_number if r.building_number else "" for r in results]
+
+    if add_position:
+        # 1-based position for Excel users; empty when not found
+        df[pos_col] = [
+            (r.number_index + 1) if (getattr(r, "number_index", None) is not None) else ""
+            for r in results
+        ]
@@
     # Identify new columns
     new_cols = [out_col]
+    if add_position:  new_cols.append(pos_col)
     if add_full_addr:  new_cols.append("FULL_ADDRESS")
     if add_confidence: new_cols.append("BN_CONFIDENCE")
     if add_lang:       new_cols.append("BN_LANG")
     if add_method:     new_cols.append("BN_METHOD")
@@
     # Colour-code BUILDING_NUMBER column: highlight empty cells
     if out_col in header:
@@
             cell.alignment = Alignment(horizontal="center")
+
+    # Style BUILDING_NUMBER_POS column
+    if add_position and pos_col in header:
+        pcol = header[pos_col]
+        for row_idx in range(2, ws.max_row + 1):
+            cell = ws.cell(row=row_idx, column=pcol)
+            cell.alignment = Alignment(horizontal="center")
+            cell.font = Font(name="Calibri", size=11)
@@
 def _parse_args():
@@
     p.add_argument("--out-col",       default="BUILDING_NUMBER",help="Output column name for building number")
+    p.add_argument("--pos-col",       default="BUILDING_NUMBER_POS",help="Output column name for building number position")
+    p.add_argument("--no-position",   action="store_true",      help="Omit BUILDING_NUMBER_POS column")
@@
     process_excel(
@@
         col4            = args.col4,
         out_col         = args.out_col,
+        pos_col         = args.pos_col,
+        add_position    = not args.no_position,
         add_confidence  = not args.no_confidence,
         add_lang        = not args.no_lang,
         add_method      = not args.no_method,
         add_full_addr   = not args.no_full_addr,
     )

diff --git a/DAT_processor.py b/DAT_processor.py
index 5555555..6666666 100644
--- a/DAT_processor.py
+++ b/DAT_processor.py
@@
-        # Append a final column with the building number or empty string
-        bn_value = result.building_number if result.building_number else ""
-        fields_out = fields + [bn_value]
+        # Append two final columns:
+        # 1) building number
+        # 2) position (1-based) of first char in LN3+LN4 concatenation
+        bn_value = result.building_number if result.building_number else ""
+        bn_pos   = (result.number_index + 1) if getattr(result, "number_index", None) is not None else ""
+        fields_out = fields + [bn_value, bn_pos]
